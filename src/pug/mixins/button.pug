mixin button(srcImg, text, selector, size, isDisabled)
  button(class = 'button-' + selector + ' button-' + size, disabled = isDisabled) #{text}
    if (srcImg)
      img(src=srcImg alt='Image')
    else