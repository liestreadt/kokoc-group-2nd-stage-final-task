mixin createButton(text, selector, size, isDisabled)
  button(class = 'button-' + selector + ' button-' + size, disabled = isDisabled) #{text}

- var message = 'Click Me HARD!'  
+createButton(message, 'warning', 'medium', false)